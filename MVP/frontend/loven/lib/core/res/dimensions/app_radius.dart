/// Corner radius tokens — soft gallery feel, not overly bubbly.
class AppRadius {
  AppRadius._();

  /// Small controls, icon buttons, compact chips.
  static const double xs = 6;

  /// Inputs, small cards, thumbnails.
  static const double sm = 8;

  /// Standard cards, buttons, text fields.
  static const double md = 12;

  /// Large cards, modals, panels.
  static const double lg = 16;

  /// Hero containers, bottom sheets (top corners).
  static const double xl = 20;

  /// Full pill / circular chip shape.
  static const double pill = 100;
}
